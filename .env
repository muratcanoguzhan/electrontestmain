# Demo OAuth Configuration
# This is a placeholder configuration for demonstration
# Replace with your actual OAuth provider settings to test real authentication

# For testing: Create a real OAuth app at https://console.developers.google.com
# and add "myapp://callback" as a valid redirect URI
OAUTH_CLIENT_ID=test-client-id-replace-with-real-one
OAUTH_AUTH_URL=https://accounts.google.com/o/oauth2/v2/auth
OAUTH_TOKEN_URL=https://oauth2.googleapis.com/token

# The app will still run with these placeholder values, but OAuth login will fail
# until you provide real credentials from your OAuth provider